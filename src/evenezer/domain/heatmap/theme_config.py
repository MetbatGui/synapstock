# 테마 계층 구조 및 설정
# Domain Logic으로서 테마의 관계를 정의합니다.

# Child Theme -> Parent Theme Mapping
THEME_HIERARCHY = {
    "리튬": "2차전지",
    "2차전지(종합)": "2차전지"
}

# 우선순위 테마 (이 순서대로 먼저 종목을 할당함)
# 일반적인 분류("2차전지")보다 구체적인 분류("리튬")를 먼저 처리하여
# 교집합 종목이 구체적인 분류에 속하도록 함.
PRIORITY_THEMES = [
    "리튬"
]

# 테마명 변경 (파일명 -> 표시명)
# 상위 그룹명("2차전지")과 하위 테마명("2차전지")이 같으면 충돌이 발생하므로
# 하위 테마명을 변경하여 처리
THEME_RENAME = {
    "2차전지": "2차전지(종합)"
}
